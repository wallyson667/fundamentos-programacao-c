#ifndef CALCUALDORA_H
#define CALCUALDORA_H
#include <stdio.h> 

// Prototipos de funções --> APENAS DECLARAÇÕES, sem o corpo
float add(float a, float b);
float subtract(float a, float b);
float multiply(float a, float b);
float divide(float a, float b);

#endif // CALCUALDORA_H